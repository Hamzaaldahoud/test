/**
 * @file
 * Behaviors for the new_theme theme.
 */

(function ($, _, Drupal) {
  Drupal.behaviors.new_theme = {
    attach() {
      // new_theme JavaScript behaviors goes here.
    },
  };
})(window.jQuery, window._, window.Drupal);
